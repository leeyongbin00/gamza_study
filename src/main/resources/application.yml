# src/main/resources/application.yml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/notesdb?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: dldydqls246!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
